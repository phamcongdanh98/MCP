# HTTP mode only. Use a random value of at least 32 characters.
WORKSPACE_MCP_TOKEN=replace-with-a-random-token

# Comma-separated executable basenames allowed in command mode.
WORKSPACE_MCP_ALLOWED_COMMANDS=git,node,npm,npx
